fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pillow==10.2.0
numpy==1.26.3
# ⚡ PyTorch with CUDA 12.4 support for GPU acceleration (compatible with CUDA 12.6 driver)
# Version 2.4.1 is the latest stable version compatible with basicsr 1.4.2
# Install with: pip install torch==2.4.1 torchvision==0.19.1 --index-url https://download.pytorch.org/whl/cu124
torch==2.4.1
torchvision==0.19.1
realesrgan==0.3.0
basicsr==1.4.2
# Fix for torchvision compatibility issue
opencv-python==4.9.0.80
# IMPORTANT: We use CodeFormer's facelib (copied to backend/facelib/)
# instead of facexlib from PyPI because CodeFormer's version has:
#   - face_upsampler parameter in paste_faces_to_input_image()
#   - adain color transfer for grayscale images  
#   - dlib face detector support
# facexlib==0.3.0  # NOT USED - using CodeFormer's facelib instead
gfpgan==1.3.8
# CodeFormer: Uses minimal architecture in codeformer_minimal/ with facelib
# Downloads 3 models on first run:
#   - codeformer.pth (restoration)
#   - codeformer_colorization.pth (colorization)
#   - codeformer_inpainting.pth (inpainting)
python-dotenv==1.0.0
pytest==8.0.0
pytest-asyncio==0.21.2
httpx==0.26.0
hypothesis==6.98.0
prometheus-client==0.19.0
sse-starlette==2.1.0
# --- Sprint 2: AWS Cognito JWT auth ---
python-jose[cryptography]==3.3.0
# --- Sprint 3: Database (RDS Postgres) + Alembic ---
sqlalchemy[asyncio]==2.0.32
asyncpg==0.29.0
alembic==1.13.2
